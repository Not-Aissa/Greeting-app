import GreetMessage from "../classes/GreetMessage";
import GreetName from "../classes/GreetName";

type GreetOptions = {
  greetMessage: GreetMessage;
  greetName: GreetName;
};

export default GreetOptions;
